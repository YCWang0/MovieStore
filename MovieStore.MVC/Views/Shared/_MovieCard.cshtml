@model MovieStore.Core.Entities.Movie
    <div class=" col-6 cl-lg-3 col-sm-4 col-xl-2 ">
        
        <a href="http://localhost:63474/movies/detail?Id= + @Model.Id">
            <img src="@Model.PosterUrl" class="img-fluid" alt="@Model.Title" />
        </a>
    </div>

